{
  "name": "@astra/stt",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Task Dock の音声入力。VAD / endpointing / provider 差し替え口。正本 §11.1。",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@astra/contracts": "workspace:*"
  },
  "devDependencies": {
    "vitest": "catalog:"
  }
}
