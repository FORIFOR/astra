{
  "name": "@astra/oauth",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "端末で走らせる OAuth2 + PKCE。正本 §21 Credential。トークンは keychain へ、サーバへは参照だけ。",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@astra/contracts": "workspace:*"
  },
  "devDependencies": {
    "vitest": "catalog:"
  }
}
