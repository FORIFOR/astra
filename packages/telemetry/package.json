{
  "name": "@astra/telemetry",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Structured logging, tracing, metrics and audit-event emission helpers.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@astra/contracts": "workspace:*",
    "@astra/db": "workspace:*",
    "@opentelemetry/api": "^1.9.1",
    "kysely": "catalog:",
    "pino": "^10.3.1"
  },
  "devDependencies": {
    "vitest": "catalog:"
  }
}
