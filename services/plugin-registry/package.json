{
  "name": "@astra/service-plugin-registry",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Plugin catalog, signatures, versions, compatibility and install state.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@astra/contracts": "workspace:*",
    "@astra/db": "workspace:*",
    "@astra/plugin-sdk": "workspace:*",
    "@astra/telemetry": "workspace:*",
    "kysely": "catalog:"
  },
  "devDependencies": {
    "vitest": "catalog:"
  }
}
