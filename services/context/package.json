{
  "name": "@genie/service-context",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Context Capsule assembly and world/context retrieval.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@genie/contracts": "workspace:*",
    "@genie/db": "workspace:*",
    "@genie/telemetry": "workspace:*"
  },
  "devDependencies": {
    "vitest": "catalog:"
  }
}
