reference : Astra DESIGN.md §0/§1/§4 and DESIGN_SYSTEM.md DS-01/DS-06 (2026-09-11): keep the existing surface, put controls next to their results, show recoverable failures, use stable labels.
hypothesis: distinguish original history from large captions; translate confirmed utterances incrementally, expose target language and explicit local/connected AI choice, keep prior translated rows through retries, and clear results between meetings.
measured  : current Dock 820×460pt content; idle 220×44pt. Translation runs once on selecting the tab only when translatedText is empty, defaults to English, lacks error retry and target selector, and has no session/generation isolation. Agent host is not running locally; Ollama llama3.2 is installed and available.
candidates: A = existing one-shot English/Agent path; B = per-utterance native translation coordinator with Japanese/English, selected local or connected AI engine, one in-flight request and 450-character chunks, source-linked rows and retry in the existing panel (selected); C = separate translation window (rejected).
measured follow-up: native popup language picker did not open from the nonactivating Dock, while the auto-update switch responded. Replaced the two language choices with direct buttons; used native accessibility containers to preserve individual labels and IDs. Llama3.2 omitted month/garbled Japanese; installed and compared Qwen2.5:7b locally using synthetic fixtures.
gate      : deterministic lifecycle/race/failure tests, actual Ollama Japanese↔English, real Google live transcript→translation before stop, light/dark native captures and geometry, full verify-all. Preserve unrelated changes; no commit until full verification passes.

Adoption: direct language buttons have a 32pt hit height; original text is visible while the model is pending. Mouse tested language switches, auto-update, tabs, and detach. The final 18-assertion actual Google/Ollama journey and 86 native tests passed. Added 12 component captures for pending/error/retry in light/dark at 320/720pt. Existing 10-surface comparisons per theme, 16-surface density, and six-state geometry checks passed. Source/appearance changes are recorded in the golden manifests. Full verification remains failed at AI execution, and separate signed-app dictation is blocked by accessibility permission. No commit until that gate is resolved.
