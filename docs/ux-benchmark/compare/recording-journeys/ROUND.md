reference : Astra DESIGN.md §0 and DS-01/DS-06 (read 2026-09-11); existing 220×44 idle surface, existing Quick Actions and recording state machine.
hypothesis: a stable Astra label with a directly actionable recording control prevents app switching from hiding recording; real screen-sharing evidence and in-place live reconnect remove mid-meeting dead ends.
measured  : idle 220×44 pt, context 320×52 pt; appContext with zero suggestions has no click action. Esc only collapses Quick Actions. PresentationGuard currently treats any detected foreground meeting as screen sharing. Streaming failures are terminal without a live retry.
candidates: A = current context-only Dock; B = stable idle with record button and optional context in Quick Actions, existing retry/error area (selected); C = permanently expanded action bar (rejected: user reports obstruction).
gate      : scenario checks for editor→record→captions→pause/resume→switch app→stop→Library; Esc and menu return; disconnected stream retry; permission denial and actual sharing. Native screenshots/geometry and relevant regression gates before commit. No new windows or design-token changes.

Measured result (2026-09-11): 17 journey assertions passed against real Google streaming, including offline reconnect and rapid pause/resume. Hands-on signed-app clicks covered start, captions, pause/resume, TextEdit switching and saved-meeting reopening. Idle content remains 220×44pt; actual notched panel is 220×76pt. Captured light/dark 16-surface sets; only idle differs intentionally. Adopted idle image and AX controls in the current physical display's baseline after before/after/diff review. Other 9 golden surfaces remain within their existing tolerance. Full verification remains red at E2E-001 dictation; no commit. Scope and unresolved real-service checks: `docs/evidence/recording-journeys/RESULTS.md`.

Density check: idle background ratio increased from 83.8% to 89.1% after removing shortcut-badge fills. DESIGN.md §4 explicitly permits updating only an affected density baseline when decorative fills were removed. The two shortcut-badge fills were removed, with the same 220×44pt footprint and both action labels retained. Adopted 89.1% for this environment’s idle baseline only; the other surfaces and thresholds remain unchanged. Revalidate against fresh captures. Geometry and both 10-surface golden comparisons previously passed.
