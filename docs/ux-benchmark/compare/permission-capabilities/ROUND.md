reference : User-provided Permission → Success → Try it specification (2026-09-11); Apple AXIsProcessTrustedWithOptions, CGPreflightScreenCaptureAccess, AVCaptureDevice.requestAccess documentation. Use explicit, purpose-specific OS requests and confirm with preflight; no automatic grant or mandatory drag.
hypothesis: Connect the existing three capability mappings to a cancellable explanation, the existing compact right-bottom guide, and an explicit continuation. Hide only idle Astra surfaces during System Settings guidance. Make file dragging an opt-in missing-app repair. Keep additional permissions collapsed in Settings.
measured  : Existing Settings content 460×620pt; guide card 300pt plus 12pt outer padding, fixed right/bottom inset 24pt, fitted height. Existing guide immediately requests permission and displays the app-file repair at every step; normal app menu starts a three-permission sequence.
candidates: A = current immediate three-step menu/always-visible drag. B = three capability choices, one purpose at a time, zero OS requests before Enable, 300pt fitted guide, repair collapsed, explicit Try it. C = mandatory setup wizard on launch (rejected: unnecessary requests). Adopt B if gates pass; retain spacing/radius/material tokens.
gate      : State-machine tests for cancel, duplicate/revoked/stale grants and one-shot continuation; microphone stays closed before permission; native light/dark purpose/waiting/help/ready/settings captures and geometry; local demos do not send data or incur model fees; permission-JIT/type/token/UI lint; verify-all before any commit. Real OS grant remains user-controlled and cannot be inferred from switch appearance.

First native measurement: purpose cards 312×189pt, waiting/ready 312×154pt, repair 312×272pt. Settings 460×620pt leaves approximately 100pt below the final privacy line after collapsing extra permissions. Follow-up candidate B uses its fitted content height (same 460pt width), resizes when the extra-permission disclosure opens, and captures both states. Practice preview also resizes from measured content when a screenshot is inserted, preventing clipped controls.
