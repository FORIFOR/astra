# 自動評価が見つけたもの（と、見つけ損ねたもの）

日付順。**仕掛けの不備を製品の欠陥として数えない**ため、両方を同じ表に並べる。

## 2026-09-01 初回

| # | 見えたこと | 実際 | どうしたか |
| --- | --- | --- | --- |
| 1 | 待機中の Astra が画面でほぼ見えない | **本当**。他アプリ（DeepNote Desktop）の常時最前面ウィジェットに 93% 覆われていた | `calm` に coverage を足して測り続ける。CALMNESS の平均は 5.3% |
| 2 | 止めるボタンが小さい | **本当**。`stopAgent` 29x16pt。隣の `openWorkspace` は 178x28 | 作りを揃えて 49x28 にした |
| 3 | 「窓が二度と戻らない」 | **仕掛けの不備**。検査用アプリが 60 秒で終了していた | 既定を 900 秒に。終了していたら `HARNESS_EXPIRED` と言う |
| 4 | 「⌥Space が効かない」 | **仕掛けの不備**。検査経路がショートカットを登録していなかった | `idle-hold` で本番と同じ登録をする。登録できたか（`shortcut=true`）を出す |
| 5 | 待機中の HUD が「⌥ space」を無条件に案内 | **半分本当**。この機械では登録できているので実害は無いが、登録に失敗した場合は効かない案内になる | 案内は権限の preflight ではなく**実際に登録できたか**で出し分ける |
| 6 | 正解画像が 7.58% ずれた | **仕掛けの不備**。採取側がショートカットを登録しておらず、実利用者が見ない姿を基準にしかけた | 採取（golden / geometry）も本番と同じ状態にする |

| 8 | 撮った絵が全部白紙になった | **道具の不備**。AX で押した先がシークレットモードを有効にし、窓が撮影に写らなくなった（設定として残る） | `guard.sh` を足し、シークレットモードと白紙の絵を判定前に弾く |
| 7 | 「Astra がクリックを一切受け付けない」 | **道具の不備**。この環境では**合成クリックが届かない**（カーソルは動き、鍵盤は届くのに、ボタンだけ届かない）。自分で作った的で確かめた | `blind.sh selfcheck` を足し、届かないときは `click` を拒む。Blind Operator は鍵盤だけで進める |

## 学んだこと

**8 件のうち 5 件が仕掛けの不備だった。**

とくに 7 は危なかった。実装を知らない評価者が 2 回続けて
「6 つのボタンを押したが何も起きない」と報告し、そのまま信じれば
「クリックが効かない」という**存在しない欠陥**を直しにいくところだった。
自分で作った的（`clicktest`）を押して初めて、届いていないのは道具のほうだと分かった。

検査の環境が製品と少しでも違うと、その差を製品の欠陥として記録する。
自動で直す仕組みを回すと、そのまま「仕掛けの穴を埋めるためのコード変更」を
生んでしまう。**直す前に、まず仕掛けが本番と同じかを確かめる。**

そのために足したもの:
- `idle-hold` は本番と同じ登録をし、`shortcut=true/false` を出す
- `shot` は「アプリが終わっている」と「窓が無い」を区別する
- 採取（golden / geometry）も本番と同じ状態にする
- Judge には OCR 照合があり、画面に無いものを根拠にした採点は捨てる

## Trust の A/B/C（1 周目・自動探索）

Judge 3 体 × 4 案。実装を見せず、画像だけ。

| | base | A 行の下に出所 | B 根拠を先に見せる | C 会議から ✓ |
| --- | --- | --- | --- | --- |
| provenance_comprehension | 5.0 | 6.0 | 6.0 | 5.0 |
| source_discoverability | 4.0 | **6.0** | 6.0 | 6.0 |
| correction_discoverability | 2.0 | 2.0 | **6.0** | 3.0 |
| verification_cost | 4.0 | 6.0 | 6.0 | 6.0 |
| confidence_to_share | 4.0 | 5.0 | 4.0 | 4.0 |
| **Trust 平均** | 3.80 | **5.00** | 5.60 | 4.80 |
| 壊れ: clarity | 6.0 | 6.0 | **5.0** | 6.0 |
| 壊れ: hierarchy | 6.0 | 6.0 | **5.0** | 6.0 |
| 壊れ: density | 4.0 | 6.0 | 5.0 | 6.0 |

**採用は A。** B は Trust が最も上がった（+1.80）が、clarity と hierarchy を
1.0 ずつ落としたので規則違反で不採用。1 つ上げて 1 つ下げたものを
「改善した」と言わないための規則が、実際に働いた。

### 2 周目: 訂正の道（correction_discoverability 2.0）

| | base(A) | D 同じ行に「直す」 | E 右端に鉛筆 | F 右端に囲みボタン |
| --- | --- | --- | --- | --- |
| correction_discoverability | 2.0 | **6.0** | 5.0 | **6.0** |
| Trust 平均 | 5.00 | 5.80 | 5.20 | 5.80 |
| 壊れ: clarity | 6.0 | 6.0 | 5.0 | 5.0 |
| 壊れ: hierarchy | 6.0 | **5.0** | **5.0** | **5.0** |
| 壊れ: density | 6.0 | **5.0** | **5.0** | **5.0** |

**全案 revert。** 訂正の道は確かに見えるようになった（2.0 → 5〜6）が、
3 案とも hierarchy と density を 1.0 落とした。行ごとに訂正の入口を常設すると、
面が混む——Judge 9 体が揃ってそう言っている。

### ここで分かった、計測のほうの限界

base では訂正は**行を押した先**に在る。静止画 1 枚にはそれが写らないので、
Judge は「無い」と読む。**2.0 は製品の点ではなく、計測の点だった。**

一枚の絵で測れるのは「見えているか」までで、「辿れるか」は測れない。
辿れるかは、実装を知らない評価者に**実際にやらせて**測る（`hold-meeting` と
Vision→AX の press を足したのはこのため）。

### 次に直すもの

`correction_discoverability` が base 2.0 / A 2.0 のまま。
**直す道が見えていない。** B は 6.0 にできたので、B の混雑を持ち込まずに
同じ効果を出す案を次に探す。

## まだ測れないもの（PASS の根拠にしない）

- VoiceOver の読み上げ順・focus の見え方・Reduce Motion
- キーボードと pointer の横取り
- 実際のマウス当たり判定（この環境では合成クリックが届かない。`blind.sh selfcheck` で毎回確かめる）
- クリックでしか辿れない道（同上。鍵盤で辿れる範囲だけが Blind の対象になる）
