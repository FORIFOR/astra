# UI taste review — 2026-09-10

This review covers only existing usage counted by `scripts/verify-ui-taste.sh`. It does not change UI code or the Craft Freeze. The reviewed additions are content-bound to the exact source hashes in `ui-taste-review.json`.

The two material uses are transient permission guidance overlays. The six capsule uses are compact status, metadata, context, profile, or voice indicators; they are not additional cards or new surfaces. Removing them would erase state meaning or make the permission guidance indistinguishable from the underlying surface.

The review accepts exactly the excess over the established ceilings: two material uses and six capsule uses. Any source edit invalidates the corresponding hash and reopens the review.
