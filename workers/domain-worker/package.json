{
  "name": "@genie/worker-domain",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Temporal worker hosting domain-agent (plugin) activities.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@genie/agent-sdk": "workspace:*",
    "@genie/contracts": "workspace:*",
    "@genie/telemetry": "workspace:*",
    "@genie/service-task": "workspace:*"
  },
  "devDependencies": {
    "vitest": "catalog:"
  }
}
