# infra/terraform

GCP infrastructure as code. Not started in Phase 0.

Planned modules (product spec §16.2):
Cloud Run services, Cloud SQL (PostgreSQL + pgvector), Memorystore (Redis),
GCS buckets, Pub/Sub topics, Secret Manager, KMS keyrings, Artifact Registry.
