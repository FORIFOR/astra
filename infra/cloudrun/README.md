# infra/cloudrun

Per-service Cloud Run deployment descriptors and Dockerfiles.
Not started in Phase 0 — Phase 0 runs everything locally via
`infra/docker-compose.dev.yml` plus `pnpm --filter <service> dev`.
