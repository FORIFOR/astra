{
  "name": "@genie/share-web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Public artifact viewer for share links (password / expiry / watermark).",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@genie/contracts": "workspace:*",
    "react": "catalog:",
    "react-dom": "catalog:",
    "@genie/api-client": "workspace:*",
    "@genie/ui-kit": "workspace:*"
  },
  "devDependencies": {
    "@types/react": "catalog:",
    "@types/react-dom": "catalog:",
    "@vitejs/plugin-react": "catalog:",
    "vite": "catalog:",
    "vitest": "catalog:",
    "@testing-library/react": "^16.3.0",
    "@testing-library/user-event": "^14.6.0",
    "happy-dom": "^20.0.0"
  }
}
