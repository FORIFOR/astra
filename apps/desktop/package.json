{
  "name": "@astra/desktop",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Astra desktop client (Tauri v2 + React). Hosts the 4-tab shell and Task Dock.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview",
    "tauri": "tauri"
  },
  "dependencies": {
    "@astra/contracts": "workspace:*",
    "@astra/ui-kit": "workspace:*",
    "react": "catalog:",
    "react-dom": "catalog:"
  },
  "devDependencies": {
    "@types/react": "catalog:",
    "@types/react-dom": "catalog:",
    "@vitejs/plugin-react": "catalog:",
    "vite": "catalog:"
  }
}
